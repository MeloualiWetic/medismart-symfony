{% extends 'base.html.twig' %}

{% block title %}DetailConsultation{% endblock %}

{% block body %}
    <h1>DetailConsultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_consultation.id }}</td>
            </tr>
            <tr>
                <th>Frais</th>
                <td>{{ detail_consultation.frais }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_consultation_index') }}">back to list</a>

    <a href="{{ path('detail_consultation_edit', {'id': detail_consultation.id}) }}">edit</a>

    {{ include('detail_consultation/_delete_form.html.twig') }}
{% endblock %}
