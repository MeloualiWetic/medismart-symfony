{% extends 'base.html.twig' %}


{% block title %}Edit Prestation{% endblock %}

{% block body %}
    <div class="row top-actions">
        <div class="col-8 top-header">
            <h4>Edit Prestation</h4>
        </div>

    </div>

    {{ include('prestation/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('prestation_index') }}">back to list</a>

{#    {{ include('prestation/_delete_form.html.twig') }}#}
{% endblock %}
