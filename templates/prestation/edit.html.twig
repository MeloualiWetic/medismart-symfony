{% extends 'base.html.twig' %}


{% block title %}Editer Prestation{% endblock %}

{% block body %}
    <div class="row top-actions">
        <div class="col-8 top-header">
            <h4>Editer Prestation</h4>
        </div>

    </div>

    {{ include('prestation/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="btn btn-secondary" href="{{ path('prestation_index') }}">Annuller</a>

{#    {{ include('prestation/_delete_form.html.twig') }}#}
{% endblock %}
