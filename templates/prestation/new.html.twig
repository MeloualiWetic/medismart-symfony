{% extends 'base.html.twig' %}

{% block title %}Créer Prestation{% endblock %}

{% block body %}
    <div class="row top-actions">
        <div class="col-8 top-header">
            <h4>Créer Prestation</h4>
        </div>

    </div>

    {{ include('prestation/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('prestation_index') }}">Annuler</a>
{% endblock %}
